#Snap2MIDI
Snap2MIDI is an implementation that takes a "short snap" of audio and converts that to MIDI. This work will cover representations such as "MelSpectrograms", "CQTs" and latent space representations of models such as DAC and ControlTransferDiffusion.
