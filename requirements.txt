argbind
librosa
mir_eval
# nnaudio2 requires numpy<2. audiomentations>=0.35 pulls numpy-rms/numpy-minmax,
# which require numpy>=2, so the augmentation pipeline is pinned to the last
# release compatible with that constraint. 0.34.1 is also contemporaneous with
# Edwards et al., whose pipeline snap2midi/utils/augmentator.py reproduces.
numpy<2
audiomentations==0.34.1
pretty_midi
torch
torchaudio
tqdm
jams @ git+https://github.com/marl/jams.git
mypy
wandb
gradio
pillow
h5py
torchlibrosa
torchinfo
sphinx
sphinx-rtd-theme
pytorch-lightning
nnaudio2
ncls
moduleconf
torch-optimizer
tensorboard
